apply plugin: 'com.android.library'
apply plugin: 'com.github.dcendents.android-maven'

group = 'com.github.vmarch'

android {
    compileSdkVersion 30
    buildToolsVersion '30.0.1'

    defaultConfig {
        minSdkVersion 15
        targetSdkVersion 30
        versionCode 34
        versionName '1.1.3'
    }
}
